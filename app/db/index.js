
const { vol, volTrack } = require('./vol');
const { single } = require('./single');
const { article, articleTrack } = require('./article');

module.exports = {
  vol,
  volTrack,
  single,
  article,
  articleTrack
};
